function branch() {
  b=$(git rev-parse --abbrev-ref HEAD 2> /dev/null)
  [[ "$b" != "" ]] && echo " $b"
}

setopt prompt_subst

# prompt format
export PROMPT='%B%~%F{yellow}$(branch) %(?.%F{white}.%F{red})%(!.#.$)%f%b '
